import { Button } from "@mui/material";
import { styled } from "@mui/material/styles";

export const StyledButton = styled(Button)`
  font-family: var(--dnfbitbit-font);
  font-size: 16px;
  width: 155px;
  height: 50px;
  border-radius: 30px;
  text-transform: none;
  margin: 5px;
`;
